/**
 * @universal-protocol/sdk
 *
 * TypeScript SDK for the Universal Protocol (BRC-20 Extension on Bitcoin).
 * Re-exports everything from src/index.ts — the canonical entry point.
 */
export * from './src/index';

